# Old Sprites

These files are kept for backup/reference only.

- `enemy_reference/` contains the older single-image Astrophage enemy sprites.

Active enemy sprites live in `assets/sprites/clean/enemies/` and `assets/sprites/clean/enemies_optimized/`. Active tower sprites live in `assets/sprites/clean/towers/`.
